template(name="adminLayout")
	if isOffline
		.ui.active.dimmer
			.ui.text.loader Offline! Sem conexão com o servidor...
	if isLoading
		.ui.active.dimmer
			.ui.text.loader #{isLoading}
	unless noApp
		+sidebar
		+technotronicsSidebar
		+confirmView
	.pusher(style="background-image:url(#{appBg}) !important;")
		//" background-repeat: no-repeat !important;background-size: contain !important;")
		div(style="height:86vh !important; margin-top:5vh; overflow:auto;")
			+Template.dynamic template=menu
			.ui.basic.segment
				+Template.dynamic template=main
			+Template.dynamic template=technotronics
